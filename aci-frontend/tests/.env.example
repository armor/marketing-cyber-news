# E2E Test Environment Configuration
# Copy this file to tests/.env and fill in your values

# =============================================================================
# Test Mode
# =============================================================================
# Set to 'true' to run tests against real backend
# Set to 'false' or omit to use MSW mocks (default)
USE_REAL_BACKEND=false

# =============================================================================
# URLs (Real Backend Mode)
# =============================================================================
# Frontend URL
TEST_BASE_URL=http://localhost:5173

# Backend API URL
TEST_API_URL=http://localhost:8080

# =============================================================================
# Test Credentials (Real Backend Mode)
# =============================================================================
# IMPORTANT: These must match actual users in your backend database
# NEVER commit real credentials to version control

# Admin user (full permissions)
TEST_ADMIN_EMAIL=admin@test.com
TEST_ADMIN_PASSWORD=TestPass123!

# Marketing user (marketing + newsletter permissions)
TEST_MARKETING_EMAIL=marketing@test.com
TEST_MARKETING_PASSWORD=TestPass123!

# Viewer user (read-only permissions)
TEST_VIEWER_EMAIL=test@example.com
TEST_VIEWER_PASSWORD=TestPass123!

# =============================================================================
# MSW Mock Mode Credentials
# =============================================================================
# When USE_REAL_BACKEND=false, these hardcoded values are used:
# - Admin: admin@test.com / TestPass123
# - Marketing: marketing@test.com / TestPass123
# - Viewer: test@example.com / TestPass123
#
# These match the fixtures in src/mocks/fixtures/users.ts

# =============================================================================
# Test Configuration
# =============================================================================
# Test timeout (milliseconds)
# Real backend tests may need longer timeouts
TEST_TIMEOUT=30000

# Network timeout (milliseconds)
TEST_NETWORK_TIMEOUT=5000

# Screenshot on failure
SCREENSHOT_ON_FAILURE=true

# Video recording
RECORD_VIDEO=false

# =============================================================================
# Debugging
# =============================================================================
# Show browser window during tests
HEADLESS=true

# Slow down test execution (milliseconds)
SLOW_MO=0

# Enable verbose logging
DEBUG=false

# =============================================================================
# Backend Setup (Real Backend Mode)
# =============================================================================
# Before running tests with USE_REAL_BACKEND=true:
#
# 1. Start the backend server:
#    cd aci-backend
#    go run cmd/server/main.go
#
# 2. Ensure test database is seeded with test users
#
# 3. Create tests/.env with credentials above
#
# 4. Run tests:
#    npm run test:e2e:real
#
# =============================================================================
